[project]
name = "de-zoomcamp-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "dbt-postgres>=1.10.0",
    "pandas>=3.0.0",
    "python-dotenv>=1.2.1",
    "sqlalchemy>=2.0.46",
]
