version: 2

models:
  - name: dim_products
    descirption: Marts model describe product entity
    columns:
      - name: stock_code
        tests:
          - not_null
          - unique
      - name: description
        tests:
          - not_null